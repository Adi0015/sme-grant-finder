# Python
venv/
venv-index/
.venv*/
__pycache__/
*.pyc

# Data (large / downloaded) — ignore everything in data/ ...
data/*
# ... except the small, precious gold eval set (profiles + human labels + metrics).
!data/gold/
# but not its regenerable LLM-generation cache / transient logs
data/gold/faith_generated.json
data/gold/*.log
# ... and small illustrative samples + the cached demo shortlist (for the UI demo mode).
!data/samples/

# Reference clones (read-only, not part of this repo)
reference/

# MLflow local file store (regenerable from eval runs)
mlruns/

# Secrets
.env

# macOS
.DS_Store
